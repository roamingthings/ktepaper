
group 'de.roamingthings'
version '1.0-SNAPSHOT'
ext.programName = 'epaper'

apply plugin: 'konan'

konan.targets = ['raspberrypi']

konanArtifacts {
    program('epaper') {
        srcDir 'src/main/kotlin'

        libraries {
            allLibrariesFrom kwiringpi, wsepd
        }
    }
}